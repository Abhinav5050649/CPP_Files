#include <bits/stdc++.h>
using namespace std;

int main()
{
    // //Vector is a dynamic array

    // vector<int> v; //General declaration of vector
    // v.push_back(5);
    // v.push_back(6); //INserting elements
    // v.push_back(7);

    // // cout << v[0] << " " << v[1] << " " << v[2] << endl;

    // v.pop_back();   //Deletes last element of array
    // cout << v.size() << endl;

    // cout << endl;
    // v.push_back(0);
    // cout << v.size() << endl;
    
    // // cout << v[0] << " " << v[1] << " " << v[2] << endl;

    // //Now, to clear all elements of array(vector), we can use v.clear() commmand

    // // v.clear();
    // // cout << v.size() << endl;

    // vector<int> v(50, 7);   //Declaring an array of 50 elements initialised to 7
    
    // cout << v[23] << endl << v.size() << endl;
    
    /*
    vector<int> v1{23, 24, 35, 56};

    vector<int> v2 = {21, 22, 23, 24, 25};
    
    vector<int> v3(5, 67);

    cout << "One: ";
    for (int i = 0, n = v1.size(); i< n; i++)
    {
        cout << v1[i] << " ";
    }
    cout << endl;

    cout << "Two: ";
    for (int i = 0, n = v2.size(); i < n; i++)
    {
        cout << v2[i] <<  " ";
    }
    cout << endl;

    cout << "Three: ";
    for (int i = 0, n = v3.size(); i < n; i++)
    {
        cout << v3[i] << " ";
    }
    cout << endl;
    */
    return 0;
}