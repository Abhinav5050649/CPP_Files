#include <bits/stdc++.h>
using namespace std;

int main()
{
    // vector<int> v = {1, 2, 3};
    // int n = v.size() + 1;
    // pair<int, vector<int>> pai(1, v);

    //pair<int, int> pai = {1, 2};
    
    // pair<int, string> pai= {1, "Hello"}; 
    // pai.first = 12;
    // cout << pai.first << " " <<  pai.second << " " << endl;

    // Creating an array of pairs
    // pair<int, int> p[5];
    // p[0] = {1, 2}, p[1] = {3, 2}, p[2] = {3, 1}, p[3] = {4, 5}, p[4] = {5, 6};

    // sort(p, p+5);

    // for (int i = 0; i < 5; i++)
    // {
    //     cout << p[i].first << " " << p[i].second << endl;
    // }

    vector <pair<int, string>> v;
    
    return 0;
}