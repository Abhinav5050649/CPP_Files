#include <bits/stdc++.h>
using namespace std;

int main()
{
    // vector<int> v = {10, 15 , 20, 25, 30};
    // auto it = v.begin();

    // cout << *it << endl;
    // it++;

    // cout << *it << endl;
    // it++;

    // cout << *it << endl;
    // it-=2;

    // cout << *it << endl;

    vector<int> v;
    v.push_back(1);
    v.push_back(2);
    v.push_back(3);

    auto it = v.begin();

    cout << *it << endl;
    v.clear();
    
    return 0;
}